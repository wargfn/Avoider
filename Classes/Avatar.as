package Classes{		/*	...	author:wargfn@wargfndev.blogspot.com	*/		import flash.display.MovieClip;		public class Avatar extends MovieClip	{				public function Avatar()		{											}				public function moveABit( xDistance:Number, yDistance:Number, baseSpeed:Number ):void		{			//next line was needed for just four direction movement			//now that there is eight way movement we pass it to this			//var baseSpeed:Number = 3;			x += ( xDistance * baseSpeed );			y += ( yDistance * baseSpeed );		}			}	}