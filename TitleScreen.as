package{		/*	...	author:wargfn@wargfndev.blogspot.com	This is the MAIN Splash screen, just to see if	I can keep a roll going	*/		import flash.display.MovieClip;	import flash.events.MouseEvent;	import flash.display.SimpleButton;	import flash.events.KeyboardEvent;	import flash.ui.Keyboard;	import flash.events.Event;		public class TitleScreen extends MovieClip	{		public function TitleScreen()		{						startButton.addEventListener(MouseEvent.CLICK, onClickStart);			addEventListener( Event.ADDED_TO_STAGE, onAddToStageSpaceStart );					}				public function onClickStart(mouseEvent:MouseEvent):void		{						dispatchEvent(new NavigationEvent(NavigationEvent.START));					}				public function onSpaceStart(keyboardEvent:KeyboardEvent):void		{			if(keyboardEvent.keyCode == Keyboard.SPACE)			{				dispatchEvent(new NavigationEvent(NavigationEvent.START));				stage.removeEventListener(KeyboardEvent.KEY_DOWN, onSpaceStart);				stage.removeEventListener(KeyboardEvent.KEY_UP, onSpaceStart);			}		}				public function onAddToStageSpaceStart(event:Event):void		{			stage.addEventListener(KeyboardEvent.KEY_DOWN, onSpaceStart);		}			}	}